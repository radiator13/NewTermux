[package]
name = "termux-terminal-emulator"
version = "0.1.0"
edition = "2021"
description = "Rust implementation of Termux terminal emulator core"

[lib]
name = "termux_rs"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
